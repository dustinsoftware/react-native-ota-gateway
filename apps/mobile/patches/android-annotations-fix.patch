--- a/android/build.gradle
+++ b/android/build.gradle
@@ -18,6 +18,14 @@ allprojects {
     mavenCentral()
     maven { url 'https://www.jitpack.io' }
   }
+
+  // Force annotations 23.0.0 to resolve conflict between kotlin-stdlib 2.1.20
+  // (strictly 13.0) and kotlinx-coroutines-core 1.10.2 (23.0.0). See KT-71290.
+  configurations.all {
+    resolutionStrategy {
+      force 'org.jetbrains:annotations:23.0.0'
+    }
+  }
 }
 
 apply plugin: "expo-root-project"
