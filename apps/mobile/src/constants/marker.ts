/**
 * Rendered on the home screen and bumped to prove an OTA delivery end to end:
 * change `v1` -> `v2`, re-export, then Check -> Download -> Restart in a host
 * and watch the home screen change. See docs/development-workflow.md.
 */
export const BUNDLE_MARKER = 'OTA marker: v1';
