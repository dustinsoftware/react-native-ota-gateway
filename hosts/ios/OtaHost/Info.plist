<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- App Transport Security: permit http://localhost so the simulator can
	     reach the demo OTA servers (:3000/:3001) and Metro (:8081) directly.
	     Without NSAllowsLocalNetworking, ATS blocks cleartext and OTA fails. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
	</dict>
	<!-- Launch screen: the Expo splash for iOS is just a solid backgroundColor
	     (#208AEF). app.json configures expo-splash-screen with an image only
	     under "android", so there is no iOS splash artwork to mirror. The color
	     lives in Assets.xcassets/SplashBackground.colorset. An empty dict here
	     is also valid to Apple but renders a bare white screen. -->
	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string>SplashBackground</string>
	</dict>
	<!-- All four orientations are required, not optional: the target ships for
	     iPad (TARGETED_DEVICE_FAMILY 1,2), and an iPad app that does not support
	     every orientation cannot support multitasking. App Store upload rejects
	     it with ITMS-90474 ("you need to include all of the ... orientations to
	     support iPad multitasking"), and xcodebuild warns about it locally with
	     "All interface orientations must be supported unless the app requires
	     full screen". UIRequiresFullScreen used to be the opt-out; it is
	     defunct as of the iOS 26 SDK. Dropping iPad (family 1) would also work,
	     but the host is meant to exercise both idioms. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
